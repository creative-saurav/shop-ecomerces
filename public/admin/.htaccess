<IfModule mod_rewrite.c>
    RewriteEngine On

    # Keep existing static admin files working, but forward missing admin routes to Laravel.
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ /index.php [L]
</IfModule>
